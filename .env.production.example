# =============================================================
# Accio Downloader - Production Environment Variables
# =============================================================
#
# Copy this file to .env.production and fill in your values.
# NEVER commit .env.production to Git.
#
# Used by: docker compose --env-file .env.production up
# =============================================================

# Database (SQLite inside the /data volume)
DATABASE_URL=sqlite:////data/sql_app.db

# Path to cookies file inside the container (mapped from ./data/cookies.txt)
COOKIES_FILE=/data/cookies.txt

# Download directory inside the container (mapped to NAS path in compose)
TEMP_DOWNLOAD_DIR=/data/downloads

# Comma-separated list of allowed CORS origins for the backend
# Add your frontend's Cloudflare Tunnel public URL here
CORS_ORIGINS=https://accio.yourdomain.com,http://localhost:3000

# Public URL of the BACKEND API (used by Next.js at build time)
# This must be the full base URL with /api/v1 suffix
NEXT_PUBLIC_API_URL=https://accio-api.yourdomain.com/api/v1
